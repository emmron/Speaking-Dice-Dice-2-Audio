<resources>

    <!-- F1 dark theme — no action bar (we handle title in each layout) -->
    <style name="AppTheme" parent="Theme.AppCompat.NoActionBar">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:windowBackground">@color/f1_black</item>
        <item name="android:textColorPrimary">@color/f1_white</item>
        <item name="android:textColorSecondary">@color/f1_white</item>
    </style>

</resources>
